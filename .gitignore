# Video, audio, and large data files — never commit these
*.mp4
*.mov
*.MOV
*.wav
*_poses.json
*_report.txt
*_gap_analysis.txt
*_practice_notes.txt

# Personal video folders
me/
pros/

# Personal pro-reference config (keep only pro_refs.example.json in the repo)
pro_refs.json

# Model weights
yolov8*.pt

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Claude / local tool config
.claude/

# OS / editor noise
.DS_Store
Thumbs.db

# Keep the wcs-analyze-skill source tree
!wcs-analyze-skill/

# Local push artifacts / backups — never commit
.git_payload.json
push_to_github.ps1
.git_corrupt_bak/
